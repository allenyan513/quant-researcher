"""MG — factor/signal research: IC, quantile spreads, decay.

`factors.py` = the factor REGISTRY (what to test); `panel.py` = warehouse I/O +
point-in-time factor/forward-return panels; `engine.py` = `run_signal` (the
single entry point) + the IC/quantile/decay math + persistence.
"""
